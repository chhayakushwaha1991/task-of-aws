1>Go to IAM console and create a policy with full access to s3 service
2> create a role with a ec2 and add the above policy into it.
3> create a s3 bucket in s3 console
4>create an ec2 instance and add the above role into it.
5> take ssh of the above instance 
6>install aws cli in the instance
 #aws install awscli -y
7>  To copy the files from EC2 to S3
# aws s3 cp <Fully Qualified Local filename> s3://<S3BucketName>
